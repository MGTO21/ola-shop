{
  "name": "ola-shop-v2",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "version": "0.0.1",
  "description": "A starter for Medusa projects.",
  "author": "Medusa (https://medusajs.com)",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --workspace=ola-shop-backend",
    "build": "npm run build:backend && npm run build:storefront",
    "build:backend": "npm run build --workspace=ola-shop-backend",
    "build:storefront": "npm run build --workspace=ola-shop-storefront",
    "deploy": "git pull origin main && npm run build && pm2 restart all"
  }
}