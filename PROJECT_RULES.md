# 📜 قواعد المشروع (Project Rules) - Ola Shop v2.0

هذا الملف يحتوي على القواعد الأساسية والمعلومات التقنية للمشروع للرجوع إليها مستقبلاً.

## 🏗️ البنية التحتية (Architecture)
- **Backend:** [Medusa.js v2](https://docs.medusajs.com) (Node.js/Express)
- **Frontend:** [Next.js 15](https://nextjs.org) (React 18, TypeScript)
- **Database:** PostgreSQL (إلزامي لـ Medusa v2)
- **Caching:** Redis (مطلوب للعمليات الخلفية)
- **Language Support:** دعم كامل للغة العربية (RTL) والحروف اللاتينية (LTR).

## 📂 هيكلة المجلدات (Directory Structure)
- `backend/`: يحتوي على خادم Medusa والإعدادات الخاصة بقاعدة البيانات والموديلات.
- `apps/storefront/`: يحتوي على واجهة المستخدم المبنية بـ Next.js.
- `public/`: الأصول العامة (صور، أيقونات).
- `.env`: ملف إعدادات البيئة (Backend).
- `apps/storefront/.env.local`: ملف إعدادات البيئة (Frontend).

## 🔌 المنافذ الأساسية (Key Ports)
- **Backend API:** `http://localhost:9000`
- **Admin UI:** `http://localhost:7001`
- **Storefront:** `http://localhost:3000`

## 🛡️ القواعد التقنية
1. **قاعدة البيانات:** يجب استخدام PostgreSQL 15+. لا يدعم Medusa v2 نظام MySQL بشكل افتراضي.
2. **البيئة:** يتطلب المشروع Node.js إصدار 20 أو أحدث.
3. **التعديلات:** أي تعديلات في الـ Backend تتطلب إعادة تشغيل `npm run dev` في مجلد `backend`.
4. **التخزين:** الصور يتم تخزينها محلياً أو عبر S3/R2 حسب الإعدادات في `medusa-config.js`.

## 🚀 التشغيل المحلي (Local Commands)
### Backend:
```bash
cd backend
npm install
npm run dev
```

### Frontend:
```bash
cd apps/storefront
npm install
npm run dev
```

---
*آخر تحديث: 2026-02-11*
